---
# tasks file for roles/ansible-role-lustre_client

# actual configuration for NSS and PAM should be in another module to allow flexibility with other authentication methods

- include: redhat.yml
  when: lustre_client_enabled and ansible_os_family == "RedHat"
